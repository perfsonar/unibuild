changequote(<!,!>)dnl
changecom()dnl

# This has packaging for RPM and Debian and builds on both.

hello


# This only has Debian packaging, so it's only built there.

ifelse(PACKAGING,deb,hello-deb)


# This has packaging for RPM and Debian.  For some arbitrary
# reason, we only want it built for RPM-based systems.

ifelse(PACKAGING,rpm,hello-rpm)
